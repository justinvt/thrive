!!! 5
%html
  %head
    %meta{:content => "text/html; charset=UTF-8", "http-equiv" => "Content-Type"}/
    %title= Rails.configuration.app[:title]
    = stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true
    = javascript_include_tag 'application', 'data-turbolinks-track' => true
    = csrf_meta_tags
  %body{:id=>page_class.gsub(" ","_"), :class=>page_class.concat(params.values_at(:id,:page).map(&:to_s).join " ").gsub(" "," _")}
    #wrap
      
      .banner
        .logo
        .masthead
          - nav_bar
      .container

        = yield

    %footer.footer
      .social
        %a.facebook{:href=> "https://www.facebook.com/#{Rails.configuration.app[:contact][:facebook]}"} &nbsp;
        %a.twitter{:href=> "https://twitter.com/#{Rails.configuration.app[:contact][:twitter]}"} &nbsp;
        %a.instagram{:href=> "https://instagram.com/#{Rails.configuration.app[:contact][:instagram]}"} &nbsp;
        %a.pinterest{:href=> "https://pinterest.com/#{Rails.configuration.app[:contact][:pinterest]}"} &nbsp;
      %p
        We serve growing readers and writers out of various locations in Philadelphia and surrounding suburbs.<br/>Get in touch at thriveliteracy@gmail.com to learn how you can Thrive with
